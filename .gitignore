# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target
# Config local de Cargo (opt-in do linker lld rápido) — NÃO versionado: forçar um
# linker custom quebra CI/clones limpos, onde o binário lld ainda não existe
# quando as build-deps compilam. Gerado por scripts/setup-fast-linker (ver
# src-tauri/.cargo/config.toml.example).
src-tauri/.cargo/config.toml

# Planejamento interno (EPICs / issues) — não versionado
milestones/

# Estado interno de ferramentas de IA (Claude Code)
.claude/
