@model object

@* Razor block comment — not rendered to HTML *@

@*
   Multi-line Razor comment.
   Source: Razor syntax documentation (public).
   License: example only.
*@

<!-- HTML comment — rendered to client -->
@{
    // C# line comment
    /* C# block comment */
    var x = 1; // inline
}

@* Comment inside a code block: *@
@{
    @* nested comment *@
    var y = 2;
}

<p>@x @y</p>
